#!/bin/bash
# Description: Install default global OpenCode skills through npx skills.

# shellcheck source=setup/lib/common.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"

bash "$SETUP_DIR/apps/opencode-skills.sh"
