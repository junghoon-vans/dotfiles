#!/bin/bash
# Description: Synchronize shared Codex and Oh My Pi skills through APM.

# shellcheck source=setup/lib/common.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"

bash "$SETUP_DIR/apps/codex-skills.sh"
