#!/bin/bash
# Description: Install Oh My Pi coding agent through Bun.

# shellcheck source=setup/lib/common.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"

bash "$SETUP_DIR/apps/omp.sh"
