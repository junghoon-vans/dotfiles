#!/bin/bash
# Description: Synchronize shared agent skills through APM.

# shellcheck source=setup/lib/common.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"

bash "$SETUP_DIR/apps/agent-skills.sh"
