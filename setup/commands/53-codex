#!/bin/bash
# Description: Install Codex CLI and bootstrap LazyCodex configuration.

# shellcheck source=setup/lib/common.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"

bash "$SETUP_DIR/apps/codex.sh"
