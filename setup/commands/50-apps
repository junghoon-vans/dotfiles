#!/bin/bash
# Description: Install Oh My Zsh and Zed Gno extension support.

source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"

for script in \
    "$SETUP_DIR/apps/ohmyzsh.sh" \
    "$SETUP_DIR/apps/zed.sh"; do
    bash "$script"
done
