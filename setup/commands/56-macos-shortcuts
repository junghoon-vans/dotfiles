#!/bin/bash
# Description: Install macOS Quick Action shortcut slots.

# shellcheck source=setup/lib/common.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"

bash "$SETUP_DIR/apps/macos-shortcuts.sh" install
